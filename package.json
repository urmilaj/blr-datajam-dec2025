{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "rimraf dist && observable build && touch dist/.nojekyll",
    "dev": "observable preview",
    "deploy": "git add -f dist/ && git commit -m 'Update build for deployment' && git subtree push --prefix dist origin gh-pages",
    "observable": "observable"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.3",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0",
    "tippy.js": "^6.3.7"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
